# 🚀 Инструкция по загрузке на GitHub и настройке GitHub Pages

## Шаг 1: Создать репозиторий на GitHub

1. Перейти на https://github.com
2. Нажать **"New repository"** (зелёная кнопка)
3. Заполнить форму:
   - **Repository name:** `diamant-will` (или любое другое название)
   - **Description:** "Глоссарий и синопсис научно-фантастического произведения 'Диамант Вилл: Выбор'"
   - **Public** ✅ (чтобы работал GitHub Pages бесплатно)
   - **НЕ** добавлять README, .gitignore, license (у нас уже есть)
4. Нажать **"Create repository"**

## Шаг 2: Подключить локальный репозиторий к GitHub

Скопировать команды с GitHub (они будут показаны после создания репозитория) или выполнить:

```powershell
# Перейти в папку проекта (если не в ней)
cd "f:\Мои Книги\Диамант Вилл"

# Переименовать ветку в main (современный стандарт)
git branch -M main

# Добавить remote (заменить YOUR-USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR-USERNAME/diamant-will.git

# Загрузить код на GitHub
git push -u origin main
```

## Шаг 3: Настроить GitHub Pages

1. На странице репозитория перейти в **Settings** (вкладка вверху)
2. В левом меню выбрать **Pages**
3. В разделе **Source** выбрать:
   - **Branch:** `main`
   - **Folder:** `/ (root)`
4. Нажать **Save**
5. Подождать 1-2 минуты
6. Обновить страницу — появится ссылка на сайт:
   ```
   Your site is live at https://YOUR-USERNAME.github.io/diamant-will/
   ```

## Шаг 4: Проверить сайт

Перейти по ссылке `https://YOUR-USERNAME.github.io/diamant-will/`

Должны быть доступны:
- **Главная страница:** https://YOUR-USERNAME.github.io/diamant-will/
- **Глоссарий:** https://YOUR-USERNAME.github.io/diamant-will/Доп.материалы/Глосарий_3
- **Синопсис:** https://YOUR-USERNAME.github.io/diamant-will/Доп.материалы/СИНОПСИС_2

## 📝 Как обновлять сайт

После любых изменений в файлах:

```powershell
# Добавить изменённые файлы
git add .

# Создать коммит
git commit -m "Описание изменений"

# Загрузить на GitHub
git push
```

Сайт обновится автоматически через 1-2 минуты.

## 🎨 Дополнительные настройки (опционально)

### Сменить тему

В файле `_config.yml` заменить `theme: jekyll-theme-cayman` на одну из:
- `jekyll-theme-minimal`
- `jekyll-theme-slate`
- `jekyll-theme-midnight`
- `jekyll-theme-architect`
- `jekyll-theme-hacker`

Полный список: https://pages.github.com/themes/

### Добавить свой CSS

Создать файл `assets/css/style.scss`:

```scss
---
---

@import "{{ site.theme }}";

// Ваши стили
body {
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
}
```

## ❓ Если что-то пошло не так

1. **Сайт не открывается:**
   - Подождите 5 минут после первой настройки
   - Проверьте настройки Pages в Settings

2. **Стили не применяются:**
   - Убедитесь что `_config.yml` корректный
   - Попробуйте другую тему

3. **Кириллица криво отображается:**
   - Добавьте в начало markdown файлов:
     ```yaml
     ---
     layout: default
     ---
     ```

4. **Ссылки не работают:**
   - В markdown используйте относительные пути
   - Замените `.md` на просто имя файла без расширения

---

## ✅ Готово!

После выполнения всех шагов у вас будет:
- ✅ Репозиторий на GitHub
- ✅ Публичный сайт с глоссарием и синопсисом
- ✅ Автоматическое обновление при каждом push

🎉 Поздравляю!
